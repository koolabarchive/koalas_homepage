# Firebase 연동 설정 가이드

코드는 준비되어 있습니다. 아래 순서대로 진행하면 로그인·회원 관리·사이트 설정 동기화가 실제로 동작합니다.
(소요 시간: 약 15분)

## 1. Firebase 프로젝트 생성
1. https://console.firebase.google.com 접속 → "프로젝트 추가"
2. 프로젝트 이름 입력 (예: hanshin-psylab)
3. Google 애널리틱스는 꺼도 됩니다 → 프로젝트 만들기

## 2. Authentication (로그인) 켜기
1. 왼쪽 메뉴 빌드 → Authentication → "시작하기"
2. 로그인 방법 탭 → "이메일/비밀번호" 선택 → 사용 설정 → 저장

## 3. Firestore (데이터베이스) 만들기
1. 빌드 → Firestore Database → "데이터베이스 만들기"
2. 위치: asia-northeast3 (Seoul) 권장
3. "프로덕션 모드"로 시작

## 4. 보안 규칙 배포
1. Firestore Database → "규칙" 탭
2. 기존 내용을 전부 지우고, 프로젝트 폴더의 firestore.rules 파일 내용 전체를 붙여넣기
3. "게시" 클릭

## 5. 웹 앱 등록 및 설정값 붙여넣기
1. 프로젝트 개요 옆 톱니바퀴 → 프로젝트 설정 → 아래로 스크롤 → "앱 추가" → 웹(</>) 선택
2. 앱 닉네임 입력 (예: lab-site) → 앱 등록 (호스팅 체크는 불필요)
3. 화면에 나오는 firebaseConfig 객체를 복사
4. 프로젝트 폴더의 js/firebase-config.js 를 열어 YOUR_... 부분을 복사한 값으로 교체 → 저장

## 6. 최초 관리자 계정 만들기 (부트스트랩)
1. 사이트를 열고 (로컬에서 확인하려면 폴더에서 `npx serve` 등 간단한 서버 실행 권장)
   로그인 페이지 → "가입 신청"으로 본인 계정 가입 → "승인 대기" 안내가 뜨면 정상
2. Firebase 콘솔 → Firestore Database → 데이터 탭 → users 컬렉션 → 방금 생긴 문서 클릭
3. role 필드 값을 pending → admin 으로 수정
4. 다시 로그인하면 관리자 화면으로 진입합니다.
   이후 다른 관리자는 화면에서 "관리자로 변경" 버튼으로 지정하면 되므로,
   콘솔에서 직접 수정하는 것은 이 최초 1회뿐입니다.

## 7. Netlify 배포 시
1. https://app.netlify.com → "Add new site" → 폴더 드래그 앤 드롭(또는 Git 연동)
2. 배포된 도메인(예: xxx.netlify.app)을 복사
3. Firebase 콘솔 → Authentication → Settings → "승인된 도메인"에 해당 도메인 추가
   (이걸 빼먹으면 배포된 사이트에서 로그인이 안 됩니다)

## 동작 확인 체크리스트
- [ ] 가입 신청 → 관리자 화면 회원 관리에 "승인 대기"로 표시됨
- [ ] 승인 클릭 → 해당 계정으로 로그인 가능, 상단 메뉴가 "로그아웃"으로 바뀜
- [ ] 회원 직접 등록(사전 등록) 후 그 이메일로 가입 → 승인 없이 바로 멤버
- [ ] 관리자 화면 → 사이트 설정에서 홈 문구 수정 → 다른 브라우저(시크릿 창)에서도 반영 확인
- [ ] 일반 멤버 계정으로 admin.html 접근 → 차단되고 홈으로 이동

## 현재 연동 범위
- 완료: 로그인/가입(승인제+사전등록), 관리자 접근 보호, 회원 관리, 홈 문구·메뉴 동기화, 보안 규칙
- 다음 단계: 공지·성과·프로젝트 게시판 데이터 연동, 연구참여확인서 PDF 발급
  (해당 패널들은 연동 전까지 데모 데이터로 표시됩니다)
